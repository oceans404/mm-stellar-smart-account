{
  "name": "usdc-mm-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "MetaMask-only Stellar smart-account demo. Create / Receive / Send USDC with an Eth key as the admin. Sibling of usdc-poc; reuses sa-poc's verifier + Channels relay.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^15.1.0",
    "buffer": "^6.0.3",
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  }
}
